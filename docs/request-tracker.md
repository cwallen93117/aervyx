# Request Tracker

Last updated: 2026-03-26

Status guide:
- `Deployed` means implemented and pushed through the live frontend/deployment flow.
- `Working` means implemented and confirmed as a finished deliverable that is not a live app feature.
- `Partial` means local edits or partial implementation exist, but the work is not fully finished and/or not yet deployed.

| Status | Item | Description |
| --- | --- | --- |
| Deployed | Remove legacy deployment-platform references and deployment-specific code | Removed the retired platform-specific source files, scripts, docs, and wording from the repo. The remaining deployment story is generic to local/self-hosted Docker Compose, and leftover local artifacts were cleaned so searches no longer keep surfacing the old deployment naming. |
| Deployed | Replace the outdated todo list with the new request tracker format | Replaced the old backlog-style todo ledger with this request tracker so new asks can be tracked in one place instead of rebuilding the old backlog. |
| Deployed | Remove scores-page banner space and rename Results portal | Removed the extra section banner/header block from the scores surface so `Scoring operations` and `Results` open directly into their controls instead of wasting vertical space. Renamed `Results portal` to `Results`. |
| Deployed | Remove redundant task distance summary card | Removed the large `Distance summary` card from the Tasks page because it duplicated the existing inline distance metrics and took up unnecessary vertical space. |
| Deployed | Remove advanced settings section from Tasks page | Removed the `Advanced Settings` toggle and its expanded block from the Tasks page so the task editor stays focused on the primary workflow and uses less vertical space. |
| Deployed | Restore advanced settings to Events scoring parameters | Kept the advanced-settings block removed from the Tasks page, but restored the advanced scoring controls on the Events scoring-parameters side so the settings live on the event where they belong. |
| Deployed | Move Events advanced settings card to the right column | Changed the Events scoring-parameters layout so the advanced scoring controls sit to the right of `Formula and points` instead of below it. |
| Deployed | Convert Events scoring checkboxes to table rows | Changed the checkbox-driven scoring fields on the Events scoring-parameters page from wrapped checkbox cards into cleaner two-column rows with the label on the left and the checkbox on the right. |
| Deployed | Remove FS helper text from scoring parameters card | Removed the sentence about the FS scoring sheet and editable AirScore settings from the Events scoring-parameters card so the section reads cleaner and stays focused on the controls themselves. |
| Deployed | Sync live frontend deployment with canonical repo | Fixed the live frontend on port `3000` so it reflects the canonical repo instead of a stale WSL mirror by syncing the current frontend source into the mounted path and restarting the frontend container. |
| Deployed | Fix overlapping Events scoring-parameters layout | Fixed the overlapping labels and controls on the Events scoring-parameters page using the Claude-guided GUI direction so labels sit above inputs and the layout no longer collapses into itself. |
| Deployed | Add site-user search to Events participant intake | Added a search box to the Events participant intake form so organizers can search the full site-wide registered user directory by name, email, username, competition number, CIVL ID, or nation before adding someone to the event. |
| Deployed | Match participant search to Tasks turnpoint-search pattern | Changed the Events participant search so it follows the same styling and interaction pattern as the Tasks `Search turnpoints` card, with live result rows and direct `Add` actions. |
| Deployed | Move task selection and task actions into a top toolbar | Moved the selected-task dropdown and task action buttons out of `Task setup` and into a compact toolbar above `Task setup` and `Timing and gates`, while keeping the existing disabled states and behaviors. |
| Deployed | Replace Tasks publish controls with a single published/unpublished toggle | Combined the Tasks page `Publish task` and `Unpublish task` controls into one toggle button that switches between green `Published` and red `Unpublished`, using the same visual style pattern as the Scores official/unofficial state toggle while preserving the existing publish and unpublish actions underneath. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Split participant intake into two side-by-side cards | Changed the Events `Participant intake` area so `Add existing person` and `Create new pilot` sit as two side-by-side inner cards, and brought the pilot form styling into line with the rest of the dashboard. |
| Deployed | Flatten Events scoring parameters into three cards and normalize text inputs | Removed the `Advanced settings` wrapper from the Events scoring parameters layout and presented `Formula and points`, `Nominal values and notes`, and `Advanced scoring` as three sibling cards. Also broadened the shared form-control styling so untyped text inputs receive the same rounded control treatment. |
| Deployed | Make Events scoring card span the full workspace width | Fixed the Events `Scoring parameters` card so it uses the full content width like the `Event selection` card instead of being constrained to a half-width workspace column. |
| Deployed | Add per-leg distance column and totals to task turnpoints list | Added a `Distance` column to the compact task turnpoints table on the Tasks page, showing the center-to-center leg distance from the previous waypoint, plus `Total distance` and `Total distance optimized` below the scroll area. |
| Deployed | Make fullscreen task settings respect the selected distance unit | Updated the Tasks page so the fullscreen task editor and task distance readouts use the distance unit selected in Settings instead of hardcoded kilometers. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Make the task turnpoints builder fit within the Tasks page rail | Rebalanced the Tasks page layout so the turnpoint list gets more horizontal space, tightened the internal table column widths, and slightly reduced the map’s footprint so the turnpoint builder no longer gets cut off inside its section. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Compress task turnpoint distance labels to save space | Simplified the Tasks distance column so the header carries the `(optimized)` hint and each row shows the optimized distance in parentheses beneath the main value, instead of repeating `Turnpoint` and `Optimized` labels on every row. The total summary follows the same pattern. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Re-center and rebalance task turnpoints table columns | Reworked the compact Tasks turnpoint table so the `Type`, `Radius`, `Distance`, and `Remove` columns line up more cleanly, the `Remove` button remains fully visible, and the column headings sit centered over the corresponding row content. This pass also used a quick GUI review from the frontend lane to confirm the spacing direction. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Pull the task Remove button inward and tighten header centering | Fine-tuned the compact Tasks turnpoint table so the `Remove` button sits farther in from the panel edge and the `Type`, `Radius`, and `Distance` headers center more precisely over the row controls and values. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Limit scoring task selection to published tasks only | Updated the Scoring Operations and task-results selectors so they only list published tasks and display only the task name, preventing draft tasks from being selected for scoring workflows. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Remove compact task distance footer and add fullscreen leg-distance column | Removed the bottom total-distance footer from the compact Tasks turnpoint rail so the search panel moves up naturally, and added a mirrored `Distance (optimized)` leg-distance column to the fullscreen task-turnpoint builder. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Clear scored results when unpublishing a task | Updated the unpublish-task flow so sending a published task back to draft also clears its score results, penalties, and scoring-input selections, keeping draft tasks out of active scoring state. The backend and frontend were restarted after the change. |
| Deployed | Replace Scores publish controls with an official/unofficial task-state toggle | Updated the Scores `Operations` footer so task results are controlled by a single toggle button that shows `Official` in green or `Unofficial` in red. Toggling it now flips all scored results for the selected task between `official` and `provisional`, while unscored pilots remain `Unscored`. The Results table state column was updated to show badges instead of per-row promotion buttons, and deleting scored results still reselects each pilot's newest uploaded IGC where available. The backend and frontend were restarted after the change. |
| Deployed | Show unofficial day results and header labels in Overall results | Updated the overall-results data path so staff can still see unofficial task-day scores, and added an `Official` or `Unofficial` badge under each task header in the Overall results table so the day state lines up with the totals. This keeps the official/unofficial toggle consistent across both task and overall results views. The backend and frontend were restarted after the change. |
| Deployed | Redesign Events airspace and restricted-fields setup into one compact upload table | Reworked the Events `Airspace / Restricted Fields` tab so it uses one combined overlay-file table with columns for file name, label, format, visibility, and upload time. Uploads now start with a blank label, and each row has its own label dropdown so you can assign `Airspace` or `Restricted fields` after upload. The overlay settings row was also tightened so the visibility toggles take less space. The backend and frontend were restarted after the change. |
| Deployed | Auto-save Events overlay settings on change | Removed the `Save overlay settings` button from the Events `Airspace / Restricted Fields` tab and changed the overlay visibility toggles to save immediately when a checkbox changes. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Widen Event schedule date fields and show spelled-out dates | Updated the Event Details `Schedule` inputs so they are substantially wider and display long-form dates like `May 30, 2026` instead of the cramped dashed browser date format. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Restore the calendar picker button on Event schedule dates | Added the calendar-picker button back to the Event Details `Starts on` and `Ends on` fields while keeping the readable long-form date text. The picker now opens a real date chooser backed by a native date input, and the chosen date still displays as `Month Day, Year`. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Widen Event schedule date boxes so the full year stays visible | Tightened the schedule row label column and widened the date-control area so the long-form event dates fit cleanly beside the calendar button without clipping the year. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Rebalance Event Details input widths so Basics and Schedule fields stay inside their cards | Adjusted the Event Details form layout so the `Basics` inputs are wider and the `Schedule` date fields stay inside the schedule card instead of overflowing to the right. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Move event action buttons into Event Selection and unify their theme | Moved `Save event`, `Duplicate event`, and `Delete event` out of the Event Details form and into the Event Selection area beside `Create a New Event`, then switched that whole action row onto the shared dashboard button styles so the controls match the rest of the site. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Allow batch overlay uploads on the Events airspace page | Updated the `Upload overlay` control so it opens a multi-file picker and uploads all selected overlay files in one pass, instead of only accepting the first chosen file. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Support `*` wildcard in dashboard search boxes | Updated the live dashboard search filters so entering `*` returns the full available list instead of treating the asterisk like a literal character. This is wired for the participant directory search and the task turnpoint search, and the placeholder copy now explains the shortcut. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Keep Scores official/unofficial toggle width fixed between states | Updated the Scores `Operations` task-state toggle so `Official` and `Unofficial` render at the same button width, preventing the footer from resizing when the state changes. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Add Score task button to Scores Operations | Added a `Score task` button to the Scores `Operations` footer and wired it to the existing rescore flow so organizers can manually run scoring directly from the operations surface. The frontend was synced into the mounted WSL copy and the frontend container was restarted. |
| Deployed | Make Scores Score task button run and refresh inside Operations panel | Updated the Scores `Operations` `Score task` button to use a local panel-side rescore action that calls the task rescore endpoint, reloads the operations rows, refreshes event summary data, and shows success or error feedback directly in the Operations footer. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Move Scores task action buttons beside the task selector | Reworked the Scores `Operations` layout so `Score task`, the official/unofficial toggle, and `Delete scored task` sit directly to the right of the selected-task dropdown inside the `Task selection` card, while the bulk IGC upload/delete actions remain in the lower footer area. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Make replay slider avoid bottom-right map controls and relocate the map scale | Updated the non-fullscreen replay bar so its scrubber panel shrinks responsively and leaves space for the altitude/map pickers on the right instead of overlapping them. The map scale indicator was moved into the upper-right area without sharing the native navigation/fullscreen control lane, which restores the north-up, zoom, and fullscreen buttons on both task and scores maps. Terrain exaggeration in 3D also now follows the altitude multiplier instead of staying fixed. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Auto-select each pilot's newest IGC when clicking Score task | Updated the Scores `Operations` `Score task` button so blank rows first auto-select that pilot's most recent uploaded IGC, then run scoring and refresh the table. This keeps the button useful even when the `Status / File` selectors have been reset to blank. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Make Delete scored task clear every Status / File selector to blank | Updated the scoring reset flow so `Delete scored task` clears all scored results, penalties, and scoring-input selections, and the Operations table now returns every `Status / File` selector to the blank `Select file or status` state instead of auto-reselecting uploaded files. The backend and frontend were restarted after the change. |
| Deployed | Remove duplicate unofficial badge under pilot names on Results page | Cleaned up the Results row layout so the pilot name block no longer repeats `Unofficial` or other state badges underneath the name when the dedicated `State` column already shows that information. The name area now keeps useful finish/status text like `Goal` or `Partial` only. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Show computed start gate times in Task details | Added a generated `Start gate times` readout under the `Start gates` and `Gate interval` controls in the Tasks `Timing and gates` section so the actual gate times appear immediately, such as `1:00 PM` and `1:15 PM`. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Rework Task timing layout by task type | Updated the Tasks `Timing and gates` section so `Race to Goal with Gates` shows `Start open`, `Start close`, `Task finish`, `Start gates`, `Gate interval`, and computed gate times, while `Race to Goal`, `Elapsed Time`, and `Open Distance` show only `Task start`, `Start close`, and `Task finish`. Hidden timing fields are no longer shown for the wrong task types, but their underlying values are not auto-cleared. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Leave gate interval blank on load | Removed the gray placeholder value from the Tasks `Gate interval` field so it loads as an empty input instead of showing a ghost `15` before the user enters an interval. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Remove scoring-parameter JSON notes field from Events page | Removed the `Task penalty / notes JSON` field from the Events `Nominal values and notes` card because penalty management now lives in the `Penalty presets` section below. The updated frontend has been built, synced into the mounted WSL copy, and the frontend container has been restarted. |
| Deployed | Restyle Save penalty presets button to match dashboard theme | Updated the `Save penalty presets` button so it uses the same primary-action treatment as the rest of the dashboard instead of reading like an outlier. The updated frontend has been built, synced into the mounted WSL copy, and the frontend container has been restarted. |
| Deployed | Add load scoring parameters from previous meets | Added a `Load scoring parameters` workflow on the Events scoring page so organizers can pick a prior meet and copy that event’s scoring fields plus saved penalty presets into the current event without switching events or auto-saving. The updated frontend has been built, synced into the mounted WSL copy, and the frontend container has been restarted. |
| Deployed | Add clickable info buttons to all Events scoring parameters | Added clickable info buttons next to the scoring fields and checkboxes on the Events scoring page so clicking them opens a detailed explanation of what that setting does. The updated frontend has been built, synced into the mounted WSL copy, and the frontend container has been restarted. |
| Deployed | Make left-column scoring help popovers open into the card instead of the sidebar edge | Adjusted the Events `Formula and points` card so its scoring help popovers open to the right into the content area, preventing the copy from being cut off against the left panel edge. The updated frontend has been built, synced into the mounted WSL copy, and the frontend container has been restarted. |
| Deployed | Remove product-name references from scoring help popovers | Rewrote the scoring help-copy text so the info popovers no longer reference product-specific names and instead use neutral, grammatically cleaned-up descriptions of what each parameter does. The updated frontend has been built, synced into the mounted WSL copy, and the frontend container has been restarted. |
| Deployed | Match participant search input styling to the task turnpoint search field | Added `input[type="search"]` to the shared dashboard form-control styling so the `Add existing person` search box uses the same rounded height, padding, and field treatment as the turnpoint search and the rest of the site. The frontend was rebuilt, synced into the mounted WSL copy, and the frontend container was restarted. |
| Deployed | Show aircraft type in live and replay pilot labels | Backed out the floating aircraft-marker experiment on the map and restored stable colored circle markers for live and replay positions. The aircraft type is now shown as a short prefix on the floating pilot label instead, using the pilot's first name, and the Settings-page aircraft selector was removed for now. |
| Partial | Finish end-to-end Meshtastic and mobile-app integration | The repo already contains a live-tracking backend, MQTT ingest for Meshtastic payloads, a mesh-config endpoint, and a Flutter mobile app scaffold with BLE pairing and tracking services. What is still missing is full end-to-end validation and live product integration: confirming the mobile app builds against the current backend, verifying BLE configuration flow, and proving Meshtastic/mobile positions appear correctly in the live dashboard. |
| Deployed | Add pilot-wide personal logbook with manual flights, statistics, replay, and IGC downloads | Built a live `Logbook` dashboard section above `Settings` that lists the signed-in pilot's flights across task-uploaded IGCs, uploaded personal IGCs, and manual entries. Added a dedicated backend pilot-flight domain, logbook APIs, shared stat derivation from track data, automatic mirroring of new task IGC uploads into the logbook, manual flight creation, personal-flight replay using the existing replay map, and download support for IGC-backed flights. |
| Working | Create Claude-led logbook mockup with left-nav Logbook tab | Created a frontend-only HTML mockup for a new `Logbook` section, including a left sidebar concept where `Logbook` sits above `Settings`. This is a static design deliverable in `docs/mockups`, not a live app feature yet. |
| Working | Establish dedicated Logbook lane and backend design memo | Created a dedicated `Logbook` lane for logbook work and documented the planned backend shape in separate persistent docs so the feature direction is preserved before live implementation starts. |
